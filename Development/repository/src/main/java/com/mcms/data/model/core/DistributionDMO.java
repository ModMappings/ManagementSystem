package com.mcms.data.model.core;

/**
 * Represents all possible distribution a given mapping could be in.
 */
public enum DistributionDMO {
    BOTH,
    SERVER_ONLY,
    CLIENT_ONLY,
    UNKNOWN
}
