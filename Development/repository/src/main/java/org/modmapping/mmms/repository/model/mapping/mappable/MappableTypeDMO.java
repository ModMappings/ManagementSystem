package org.modmapping.mmms.repository.model.mapping.mappable;

/**
 * Represents a single type of a component,
 * as such it represents a type of a piece of source code that can be remapped.
 */
public enum MappableTypeDMO {
    PACKAGE,
    CLASS,
    METHOD,
    FIELD,
    PARAMETER
}
